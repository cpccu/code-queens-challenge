#include<stdio.h>
int main() {

    int score;
    scanf("%d", &score);
    if (score>=12);
        {
        printf("Yes\n", score);
    }
    else
        {
        printf("No\n", score);
    }
return 0;
}