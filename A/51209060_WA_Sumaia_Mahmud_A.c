#include <stdio.h>

int main() {
    int points;
      printf("Enter Score:");
    scanf("%d", &points);

    if (points == 17) {
        printf("YES\n");
    }
    else if (points >=20) {
        printf("YES\n");
    } else {
        printf("No\n");
 }
}
